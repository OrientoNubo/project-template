# CHANGELOG

> Project-level changelog (cross-version, structure / shared-code / infrastructure changes). For version-level updates, see `src/<version>/CHANGELOG.md`.
> Timestamp headers (`## YYYY-MM-DD-HH:MM:SS`), in descending order.
